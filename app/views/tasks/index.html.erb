<div calss="container mt-5">

  <h1>"Tasks🫧"</h1>

  <ul id="tasks">
    <% @tasks.each do |task| %>
    <li><%= task.title %></li>
    <% end %>
  </ul>
  
</div>
<%# <%= link_to task.title, task_path(task.id) %>
